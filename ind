-- 1. Таблица Guests (гости)
CREATE TABLE Guests (
    guest_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    first_name VARCHAR2(50) NOT NULL,
    last_name VARCHAR2(50) NOT NULL,
    phone VARCHAR2(20),
    email VARCHAR2(100) UNIQUE
);

-- 2. Таблица Room_Types (типы номеров)
CREATE TABLE Room_Types (
    type_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    type_name VARCHAR2(50) NOT NULL,
    description CLOB
);

-- 3. Таблица Rooms (номера)
CREATE TABLE Rooms (
    room_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    room_number VARCHAR2(10) NOT NULL UNIQUE,
    price NUMBER(10,2) NOT NULL,
    capacity NUMBER NOT NULL,
    type_id NUMBER,
    CONSTRAINT fk_rooms_type FOREIGN KEY (type_id) REFERENCES Room_Types(type_id) ON DELETE SET NULL
);

-- 4. Таблица Bookings (бронирования)
CREATE TABLE Bookings (
    booking_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    guest_id NUMBER NOT NULL,
    room_id NUMBER NOT NULL,
    check_in_date DATE NOT NULL,
    check_out_date DATE NOT NULL,
    status VARCHAR2(20) DEFAULT 'confirmed',
    CONSTRAINT fk_bookings_guest FOREIGN KEY (guest_id) REFERENCES Guests(guest_id) ON DELETE CASCADE,
    CONSTRAINT fk_bookings_room FOREIGN KEY (room_id) REFERENCES Rooms(room_id) ON DELETE CASCADE,
    CONSTRAINT chk_dates CHECK (check_out_date > check_in_date)
);

-- 5. Таблица Payments (платежи)
CREATE TABLE Payments (
    payment_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    booking_id NUMBER NOT NULL,
    amount NUMBER(10,2) NOT NULL,
    payment_date DATE DEFAULT SYSDATE,
    method VARCHAR2(30),
    CONSTRAINT fk_payments_booking FOREIGN KEY (booking_id) REFERENCES Bookings(booking_id) ON DELETE CASCADE
);
-- Добавляем типы номеров
INSERT INTO Room_Types (type_name, description) VALUES ('Стандарт', 'Однокомнатный номер, душ, телевизор');
INSERT INTO Room_Types (type_name, description) VALUES ('Люкс', 'Двухкомнатный номер, джакузи, мини-бар');
INSERT INTO Room_Types (type_name, description) VALUES ('Семейный', 'Две спальни, детская кроватка, холодильник');
INSERT INTO Room_Types (type_name, description) VALUES ('Эконом', 'Без окон, минимальный набор');

-- Добавляем номера
INSERT INTO Rooms (room_number, price, capacity, type_id) VALUES ('101', 2500.00, 2, 1);
INSERT INTO Rooms (room_number, price, capacity, type_id) VALUES ('102', 2500.00, 2, 1);
INSERT INTO Rooms (room_number, price, capacity, type_id) VALUES ('201', 5500.00, 4, 2);
INSERT INTO Rooms (room_number, price, capacity, type_id) VALUES ('202', 4200.00, 3, 3);
INSERT INTO Rooms (room_number, price, capacity, type_id) VALUES ('301', 1500.00, 1, 4);

-- Добавляем гостей
INSERT INTO Guests (first_name, last_name, phone, email) VALUES ('Иван', 'Петров', '+7-912-123-45-67', 'ivan@example.com');
INSERT INTO Guests (first_name, last_name, phone, email) VALUES ('Мария', 'Сидорова', '+7-913-987-65-43', 'maria@example.com');

-- Добавляем бронирования
INSERT INTO Bookings (guest_id, room_id, check_in_date, check_out_date, status) VALUES (1, 1, DATE '2026-05-01', DATE '2026-05-05', 'confirmed');
INSERT INTO Bookings (guest_id, room_id, check_in_date, check_out_date, status) VALUES (2, 3, DATE '2026-05-10', DATE '2026-05-15', 'pending');

-- Добавляем платежи
INSERT INTO Payments (booking_id, amount, method) VALUES (1, 10000.00, 'credit_card');
INSERT INTO Payments (booking_id, amount, method) VALUES (2, 27500.00, 'cash');




-- Все номера с их типами
SELECT r.room_number, rt.type_name, r.price, r.capacity
FROM Rooms r
JOIN Room_Types rt ON r.type_id = rt.type_id;

-- Бронирования с данными
SELECT b.booking_id, g.first_name, g.last_name, r.room_number, 
       b.check_in_date, b.check_out_date, b.status
FROM Bookings b
JOIN Guests g ON b.guest_id = g.guest_id
JOIN Rooms r ON b.room_id = r.room_id;
